import assert from 'assert';

describe('Example', () => {
  describe('action', () => {
    describe('when condition', () => {
      it('outcome', () => {
        assert(true, 'The example is working!');
      });
    });
  });
});
